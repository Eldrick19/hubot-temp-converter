// Generated by CoffeeScript 2.7.0
(function() {
  // Temperature converter class
  var Temperature;

  Temperature = class Temperature {
    constructor(tempValue) {
      this.tempValue = tempValue;
    }

    celsiusToFahrenheit() {
      return this.tempValue = (this.tempValue * (9 / 5)) + 32;
    }

    celsiusToKelvin() {
      return this.tempValue += 273.15;
    }

    celsiusToRankine() {
      return this.tempValue = this.celsiusToKelvin() * (9 / 5);
    }

    celsiusToDelisle() {
      return this.tempValue = (100 - this.tempValue) * (3 / 2);
    }

    celsuisToNewton() {
      return this.tempValue *= 33 / 100;
    }

    celsiusToReaumur() {
      return this.tempValue *= 4 / 5;
    }

    celsiusToRomer() {
      return this.tempValue = this.tempValue * (21 / 40) + 7.5;
    }

    fahrenheitToCelsius() {
      return this.tempValue = (this.tempValue - 32) * (5 / 9);
    }

    kelvinToCelsius() {
      return this.tempValue -= 273.15;
    }

    rankineToCelsius() {
      return this.tempValue = (this.tempValue - 491.67) * (5 / 9);
    }

    delisleToCelsius() {
      return this.tempValue = (100 - this.tempValue) * (2 / 3);
    }

    newtonToCelsius() {
      return this.tempValue *= 100 / 33;
    }

    reaumurToCelsius() {
      return this.tempValue *= 5 / 4;
    }

    romerToCelsius() {
      return this.tempValue = (this.tempValue - 7.5) * (40 / 21);
    }

  };

  module.exports = Temperature;

}).call(this);
